# Change 0001: Initial Product Slice

This change defines the first shippable slice for the Linux continuous CPU profiler.

It covers:

- CLI and daemon architecture
- storage model for capture slices
- a utilitarian front-end console
- a mock mode for repeatable local usage
- baseline tests

